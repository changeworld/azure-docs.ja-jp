<properties 
   pageTitle="仮想ネットワークの作成" 
   description="基本的な仮想ネットワークを簡単に作成する方法" 
   services="virtual-network" 
   documentationCenter="" 
   authors="cherylmc" 
   manager="adinah" 
   editor="tysonn"/>

<tags
   ms.service="virtual-network"
   ms.devlang="na"
   ms.topic="article"
   ms.tgt_pltfrm="na"
   ms.workload="infrastructure-services" 
   ms.date="02/20/2015"
   ms.author="cherylmc"/>

# 仮想ネットワークの作成 



仮想ネットワークを作成すると、VNet 内のサービスと VM はインターネットを経由せずに相互に安全な通信を行えるようになります。クラウドのみの専用仮想ネットワークを作成することは、それほど時間のかからない容易なプロセスです。クラウドのみの仮想ネットワークはクロスプレミス接続を目的としていないため、VPN デバイスや認証証明書を取得したり構成したりする必要がありません。 

仮想ネットワークを一度作成したら、そこに新しい VM と PaaS インスタンスを追加できます。管理ポータルを使用して VM を作成する場合は、仮想ネットワークを指定できるように、**[ギャラリーから]** を選択してください。手順の途中で元に戻ることや、作成後の VM を仮想ネットワークに配置することはできないため、この選択は重要です。

[Azure Note] **クラウドのみの専用仮想ネットワークを作成するには、以下の手順に従います。**クロスプレミス構成の作成では複雑な作業が発生するため、後で内部設置型ネットワークに接続する仮想ネットワークを作成する場合は、この手順を使用しないでください。セキュリティで保護されたクロスプレミス VPN を作成する場合は、「[セキュリティで保護されたクロスプレミス接続について](https://msdn.microsoft.com/library/azure/dn133798.aspx)」をご覧ください。

## <a name="CreateyourVNet">仮想ネットワークの作成</a>

1. **管理ポータル**にログインします。
2. 画面の左下隅で **[新規]** をクリックします。ナビゲーション ウィンドウで **[ネットワーク サービス]** をクリックし、**[仮想ネットワーク]** をクリックします。**[カスタム作成]** をクリックして、構成ウィザードを開始します。
3. **[仮想ネットワークの詳細]** ページで、次の情報を入力し、右下にある次へ進む矢印をクリックします。詳細ページの設定の詳細については、「[管理ポータルでの仮想ネットワークの構成について](https://msdn.microsoft.com/library/azure/jj156074.aspx)」の**仮想ネットワークの詳細**セクションをご覧ください。
	-  **[名前]:** 仮想ネットワークの名前を指定します。VM とサービスを展開するときはこの仮想ネットワーク名を使用するので、あまり複雑な名前にしないことをお勧めします。

	-  **[場所]:** 場所 (地域) をドロップダウン リストから選択します。仮想ネットワークは特定の地域の Azure データセンターで作成されます。



4. **[DNS サーバーと VPN 接続]** ページでは、何も変更しません。矢印をクリックして次のページに進みます。Azure には既定で、仮想ネットワーク用の基本的な名前解決サービスが用意されています。ただし、基本的な Azure の名前解決では対応できないほど名前解決の要件が複雑である場合もあります。そのような場合は、DNS を実行する仮想マシンを後で仮想ネットワークに追加することも可能です。Azure の名前解決と DNS の詳細については、「[名前解決 (DNS)](https://msdn.microsoft.com/library/azure/jj156088.aspx)」をご覧ください。 
5. **[仮想ネットワーク アドレス空間]** ページでは、新しい VNet で使用するアドレス空間を入力します。VM 用に特定の内部 IP アドレス範囲が必要な場合や、静的 DIP を受け取る VM 用に特定のサブネットを作成する場合を除き、このページで変更を行う必要はありません。複数のサブネットを作成する場合は、このページの **[サブネットの追加]** をクリックします。詳細ページの設定の詳細については、「[管理ポータルでの仮想ネットワークの構成について](https://msdn.microsoft.com/library/azure/jj156074.aspx)」の**仮想ネットワークの詳細**セクションをご覧ください。

	-  詳細ページの設定の詳細については、「[管理ポータルでの仮想ネットワークの構成について](https://msdn.microsoft.com/library/azure/jj156074.aspx)」の**仮想ネットワークの詳細**セクションをご覧ください。
	-  ここではクロスプレミス VPN 構成を使用してこの仮想プライベート ネットワークを内部設置型ネットワークに接続するわけではないので、これらの設定と既存の内部設置型ネットワークの IP アドレスの範囲を調整する必要はありません。後でクロスプレミス構成を作成する可能性がある場合は、ルーティングの問題を避けるため、アドレス空間とローカル サイト上の既存の範囲をここで調整する必要があります。後でアドレス範囲を変更すると、範囲が重複している場合は特に、作業が多少複雑になる可能性があります。


6. [仮想ネットワーク アドレス空間] ページの右下にあるチェックマークをクリックすると、仮想ネットワークの作成が開始されます。仮想ネットワークが作成されると、管理ポータルのネットワーク ページの [状態] に [作成済み] と表示されます。
7. 仮想ネットワークが作成されたら、VNet にデプロイできます。たとえば、VM を VNet にデプロイする場合は、「[Add a Virtual Machine to a Virtual Network (仮想ネットワークに仮想マシンを追加する)](http://www.windowsazure.com/manage/services/networking/add-a-vm-to-a-virtual-network/)」をご覧ください。仮想ネットワークの選択を可能にするために新しい VM を作成するときは、**[ギャラリーから]** を必ず選択します。既存の VM と PaaS インスタンスが既に展開されている場合は、それらを新しい VNet に移動する前に必要な手順があることにご注意ください。これは、必要とされるネットワーク構成の設定が展開中に追加されているからです。新しい VNet に再度展開する必要があります。



## 次のステップ
-  [Virtual Network Technical Overview (仮想ネットワークの技術的概要)](http://msdn.microsoft.com/library/windowsazure/jj156007.aspx)

 
-  [Add a Virtual Machine to a Virtual Network (仮想ネットワークに仮想マシンを追加する)](http://www.windowsazure.com/manage/services/networking/add-a-vm-to-a-virtual-network/)

-  [仮想ネットワーク FAQ](http://msdn.microsoft.com/library/windowsazure/dn133803.aspx)

-  [ネットワーク構成ファイルを使用した仮想ネットワークの構成](http://azure.microsoft.com/documentation/articles/virtual-networks-using-network-configuration-file/)

-  [Azure Name Resolution Overview (Azure 名前解決の概要)](http://go.microsoft.com/fwlink/?LinkId=248097)
 



<!--HONumber=47-->
